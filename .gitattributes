# Generated artifacts. They are rebuilt by `bun run skill:create-quiz:generate`
# and guarded against drift in CI, so there is nothing to review inside them:
# mark them generated so GitHub collapses them in pull requests and leaves them
# out of the repository language statistics. Review the sources instead —
# scripts/create-quiz-validator*.ts and src/shared/lib/quiz/schema.ts.
skills/create-quiz/scripts/validate-quiz.mjs linguist-generated=true
skills/create-quiz/references/quiz.v1.schema.json linguist-generated=true
